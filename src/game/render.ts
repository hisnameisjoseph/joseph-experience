// TODO: the only module that touches the canvas context. Draw tiles, player,
// and objects as colored rectangles; imageSmoothingEnabled = false, integer
// scaling from a low internal resolution (~320x180).
export {}
